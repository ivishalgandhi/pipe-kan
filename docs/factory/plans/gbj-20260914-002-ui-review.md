# UI Review: gbj-20260914-002 — Board | Calendar tabs

**Reviewer:** ui-review (OMP cursor/claude-4.5-sonnet)  
**Date:** 2026-09-14  
**Job card:** `/Users/vishal/code/software-factory/asf/runtime/factory-coordinator/jobs/active/gbj-20260914-002.md`

## Verdict: **PASS**

The Calendar UI implementation meets all mandatory requirements and demonstrates professional B2B product chrome consistent with the existing Board surface.

---

## Evidence

### Screenshots

1. **Board tab** (`docs/factory/plans/board-tab-screenshot.png`)
   - Three-column Kanban layout with To Do, In Progress, Done
   - Board | Calendar tabs in header, Board selected
   - Matches existing shadcn styling

2. **Calendar tab** (`docs/factory/plans/calendar-tab-screenshot.png`)
   - Full-month dayGrid calendar filling main pane
   - Board | Calendar tabs in header, Calendar selected
   - Events displayed as issue keys (DEMO-1, DEMO-2, etc.)

3. **Calendar with open pane** (`docs/factory/plans/calendar-with-open-pane.png`)
   - Calendar event clicked (DEMO-8)
   - Right detail pane (`id="open"`) opened
   - Shows issue key, summary, Jira URL, fields
   - Edit and Close buttons present

### Mandatory Requirements: All PASS

| Requirement | Status | Evidence |
|-------------|--------|----------|
| Calendar not in left sidebar | ✅ PASS | Full calendar in main canvas; sidebar has nav button only (`src/App.tsx:1823-1833`) |
| Board \| Calendar tabs in main canvas | ✅ PASS | Tab list at `src/App.tsx:1932-1964`; `role="tablist"` with proper `aria-selected` |
| Sidebar Calendar selects Calendar tab | ✅ PASS | Button at `src/App.tsx:1823-1833` calls `openCalendarTab()` (`src/App.tsx:1625-1628`), which calls `selectCalendarTab()` from `src/calendar.ts:31-33` |
| Events follow selected opener | ✅ PASS | `BoardCalendar` receives `Object.values(visible).flat()` (`src/App.tsx:2150`); `visible` computed from `combinedBoard`/`filterValue` based on `boardKind` (`src/App.tsx:1121-1131`) |
| Calendar click Opens right pane | ✅ PASS | Verified via browser test: clicking DEMO-8 event opened `id="open"` pane with issue details; `onOpen` prop wired to `open(key)` (`src/App.tsx:2151`) |
| Chrome consistency | ✅ PASS | Tabs use existing shadcn Button styling (`h-7 rounded-md px-2 text-[13px]`); same header structure as Board; no competing visual language |

### Additional Quality Checks

| Criterion | Status | Notes |
|-----------|--------|-------|
| **Hierarchy** | ✅ Good | Tabs clearly secondary to project badge; search/filter chrome aligned right |
| **Density** | ✅ Excellent | Calendar fills available height (`height="100%"` at `src/components/board-calendar.tsx:31`); no wasted vertical space |
| **Tab clarity** | ✅ Good | "Board" vs "Calendar" labels unambiguous; `aria-selected` toggles properly |
| **Combined visibility** | ✅ Good | "Combined" opener clearly listed in sidebar (`src/App.tsx:1810-1821`); glossary term should be in `CONTEXT.md` per job card |
| **Keyboard/ARIA** | ✅ Good | Tab roles (`role="tab"`, `aria-selected`); tablist (`role="tablist"`, `aria-label="Board or Calendar"`); keyboard navigation implied by native button elements |
| **Event titles** | ✅ Good | Issue keys displayed as buttons (`.fc-event-key` at `src/components/board-calendar.tsx:41`); not verbose titles |
| **Empty states** | ⚠️ Acceptable | No explicit empty state rendering in `BoardCalendar.tsx`, but FullCalendar handles gracefully when `events=[]` |
| **Month height** | ✅ Excellent | `height="100%"` with `expandRows` and no `contentHeight` clamp; fills pane, not 220px mini month |

### Epic dates in Combined view

✅ **PASS** — Epic `created` and `targetEnd` are copied into Cards at `src/visible.ts:290,293`:

```typescript
export function combinedBoard(
  columns: Record<string, Card[]>,
  epics: Epic[],
  query = "",
  opts?: VisibleOpts,
): Record<string, Card[]> {
  const hide = new Set(opts?.hide ?? []);
  const filter = opts?.filter;
  const matchedEpics = epics.filter(
    (epic) => cardMatches(epic, query) && epicMatchesFilter(epic, filter),
  );
  const epicCards: Card[] = matchedEpics.map((epic) => ({
    key: epic.key,
    summary: epic.summary,
    priority: epic.priority,
    assignee: epic.assignee,
    dueDate: epic.dueDate,
    targetEnd: epic.targetEnd,
    labels: epic.labels,
    type: "Epic",
    created: epic.created,
  }));
```

This matches the test at `src/visible.test.ts:855-879` ("combinedBoard copies Epic created and targetEnd onto Cards").

### Calendar tab persistence

✅ **PASS** — Tab selection persists via `localStorage`:
- Read: `readCanvasTab()` at `src/calendar.ts:18-24`
- Write: `writeCanvasTab(next)` at `src/calendar.ts:26-28`
- State: `useState<CanvasTab>(readCanvasTab)` at `src/App.tsx:1107`
- Persist: `persistCanvasTab(next)` at `src/App.tsx:1620-1623`

Sidebar Calendar button calls `openCalendarTab()` (`src/App.tsx:1625-1628`), which writes to `localStorage` via `selectCalendarTab()` and updates React state via `setCanvasTab`.

---

## Code References

| File | Lines | Purpose |
|------|-------|---------|
| `src/App.tsx` | 1932-1964 | Board \| Calendar tab list in main canvas header |
| `src/App.tsx` | 1823-1833 | Sidebar Calendar nav button |
| `src/App.tsx` | 2148-2153 | Conditional rendering: `canvasTab === "calendar"` shows `<BoardCalendar>` |
| `src/App.tsx` | 2150 | Calendar receives `cards={Object.values(visible).flat()}` |
| `src/App.tsx` | 1121-1131 | `visible` computed from opener (stories/epics/combined) |
| `src/components/board-calendar.tsx` | 22-58 | FullCalendar with clickable issue-key buttons |
| `src/calendar.ts` | 31-33 | `selectCalendarTab()` writes to localStorage |
| `src/visible.ts` | 273-324 | `combinedBoard` copies `created` and `targetEnd` from epics |

---

## Required Fixes

**None.** All mandatory requirements passed.

---

## Minor Observations (not blocking)

1. **Empty state:** `BoardCalendar` does not render a custom empty state when no events exist. FullCalendar shows an empty grid, which is acceptable but could be enhanced with a subtle "No events in this view" message. Not a FAIL criterion per job card.

2. **Glossary term:** Job card specifies that "Combined" should be glossaried in `CONTEXT.md`. Confirm this was done (not part of UI review scope, but noted for completeness).

3. **Tab persistence edge case:** If `localStorage` fails (private browsing), `readCanvasTab()` defaults to `"board"` (`src/calendar.ts:22`). This is safe fallback behavior.

---

## Conclusion

The Calendar UI is production-ready. The implementation:
- Moves Calendar from a sidebar widget to a main-canvas tab peer to Board
- Respects the selected opener (All stories / All epics / Combined)
- Opens issue details in the existing right pane on event click
- Preserves epic dates in Combined view
- Maintains shadcn chrome consistency

No rework required. Ready to ship.
